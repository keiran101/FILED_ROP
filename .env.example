# FileDrop 配置示例 — 复制为 .env 并按需修改

# 监听地址和端口
HOST=0.0.0.0
PORT=1158

# 上传目录（相对路径或绝对路径）
UPLOAD_DIR=uploads

# 单文件大小上限（MB）
MAX_FILE_SIZE_MB=100

# 每分钟每 IP 最多上传文件数（0 = 不限制）
RATE_LIMIT=30

# 绕过文件大小限制的密码（留空则禁用此功能）
BYPASS_PASSWORD=

# 界面显示的设备名称
DEVICE_NAME=FileDrop
